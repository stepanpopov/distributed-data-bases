# PostgreSQL Client Authentication Configuration File
# ===================================================
# This file controls: which hosts are allowed to connect, how clients
# are authenticated, which PostgreSQL user names they can use, which
# databases they can access.

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
# IPv6 local connections:
host    all             all             ::1/128                 trust

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            trust
host    replication     all             ::1/128                 trust

# ===========================================
# КОНФИГУРАЦИЯ ДЛЯ РЕПЛИКАЦИИ МЕЖДУ УЗЛАМИ
# ===========================================

# Разрешение подключений для всех узлов в Docker сети
host    hotel_management    repuser         all                     md5
host    replication         repuser         all                     md5

# Разрешение обычных подключений для администрирования  
host    hotel_management    postgres        all                     md5
host    all                 all             all                     md5